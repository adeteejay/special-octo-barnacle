---
import Filters from './Filters.astro'
---

<header class="bg-white shadow-sm sticky top-0 z-[99999]">
  <div class="max-w7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <div class="h-10">
          <span class="text-2xl font-bold text-primary">OpenRent</span>
        </div>
      </div>

      <div class="hidden md:block flex-1 max-w-md mx-auto p-4">
        <div class="flex flex-col md:flex-row gap-2 items-stretch">
          <div class="flex-1 relative">
            <input
              type="text"
              placeholder="Enter a location to search"
              class="w-full px-4 py-2 rounded-lg border border-gray-500 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
            />
          </div>

          <div class="flex gap-2">
            <button
              class="px-6 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors"
            >
              Search
            </button>
            <!-- 
            <button
              id="filterButton"
              class="p-2 border rounded-lg hover:border-primary transition-colors flex items-center"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-5 w-5"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"
                >
                </path>
              </svg>
              <span class="hidden md:inline ml-2">Filters</span>
            </button> -->
          </div>
        </div>
      </div>

      <nav class="hidden md:flex items-center space-x-8">
        <a href="#" class="text-text-secondary hover:text-primary text-base font-medium">About</a>
        <a href="#" class="text-text-secondary hover:text-primary text-base font-medium">
          Pricing & Services
        </a>
        <a href="#" class="text-text-secondary hover:text-primary text-base font-medium">Manage</a>
        <button
          class="bg-primary text-white px-4 py-2 rounded-lg hover:scale-105 transition-transform duration-100"
        >
          Add Listing
        </button>
      </nav>

      <button class="md:hidden">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16M4 18h16"
          >
          </path>
        </svg>
      </button>
    </div>
  </div>
</header>
